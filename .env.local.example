# Lucid Charts API Configuration
# Required for local development to access the Lucid API

# Option 1: Point to your deployed Vercel app's proxy (RECOMMENDED)
# This uses the secure proxy with the API key stored on the server
REACT_APP_LUCID_PROXY_URL=https://unicorn-one.vercel.app

# Option 2: Set the API key directly (for testing only)
# Note: Direct API calls from browser will fail due to CORS
# REACT_APP_LUCID_API_KEY=your-lucid-api-key-here

# Example deployed URLs:
# REACT_APP_LUCID_PROXY_URL=https://unicorn-app.vercel.app
# REACT_APP_LUCID_PROXY_URL=https://unicorn-git-main-yourname.vercel.app
